# Inputs

Put working inputs here when that is easier than referencing them elsewhere.

- `source/`: copied or linked source files
- `screenshots/`: page captures, cropped images, running-screen shots
- `notes/`: PM notes, business context, operator notes

The internal AI may also read project files outside this folder when the environment allows it.
This folder exists to keep the common case simple.
