# Internal Workspace Exports

`exports/` is reserved for generated carry-in workspaces that will be copied into the company environment.

Default export target:

- `internal-ai-workspace/`

This directory is generated by `python scripts/export_ai_pro_bundle.py`.
Do not mix AM analysis artifacts into this directory.
